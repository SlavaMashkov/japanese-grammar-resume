[project]
name = "japanese-grammar-resume"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["reportlab>=4.0"]

[project.scripts]
japanese-grammar-resume = "japanese_grammar_resume.__main__:main"
